# Application (backend)

This repo contains the backend/API and business logic.

Quick start:

```bash
cd application
npm install
npm run dev
```

Environment:
- Configure DB and auth env vars (do not commit `.env` files).

Migrations & DB:
- See the `data` repo for migrations and DB bootstrap scripts.
