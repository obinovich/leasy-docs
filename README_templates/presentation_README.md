# Presentation (frontend)

This repo contains the frontend application extracted from the mono-repo.

Quick start:

```bash
npm install
npm start
```

Environment:
- Configure API endpoint: `REACT_APP_API_URL`

Build:
```bash
npm run build
```
